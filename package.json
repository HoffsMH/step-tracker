{
  "name": "step-tracker",
  "version": "1.0.0",
  "description": "step tracker display Library",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "babel src -d dist",
    "start": "node dist/index.js",
    "go": "yarn build && yarn start"
  },
  "keywords": [
    "scss",
    "js",
    "step",
    "tracker",
    "progress"
  ],
  "author": "Matt Hecker",
  "license": "MIT",
  "devDependencies": {
    "babel-core": "^6.26.0",
    "babel-preset-env": "^1.6.1",
    "babel-preset-stage-3": "^6.24.1",
    "mocha": "^4.0.1"
  }
}
